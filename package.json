{
  "name": "olystate-pro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "server:dev": "node --env-file-if-exists=.env --experimental-strip-types server/index.ts",
    "build": "tsc -b && vite build",
    "preview": "node scripts/serve-dist.mjs dist",
    "typecheck:server": "tsc -p tsconfig.server.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test .test-build/src/domain/engine.test.js .test-build/src/domain/ecosystemEnvelope.test.js .test-build/server/connectionSettings.test.js",
    "test:e2e": "node e2e/planned-session-flow.cjs",
    "verify": "npm run typecheck:server && npm run test && npm run build"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^6.0.2",
    "@playwright/test": "^1.60.0",
    "typescript": "^5.6.3",
    "vite": "^8.0.14"
  }
}
